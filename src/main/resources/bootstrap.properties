# this should be the properties file name at the ConfigServer( we have myapp-config-default.properties in configserver)
spring.application.name=myapp-config

#config server url - where config server is running
spring.cloud.config.uri=http://localhost:8901

# we have the properties file with name myapp-config-apac.properties / myapp-config-emea.properties / myapp-config.properties
# in order to refer to apac properties
#spring.profiles.active=apac

# in order to refer to emea properties
spring.profiles.active=emea

